export type User = {
    id: string,
    email: string,
    name: string,
    password: string,
}

export type Expense = {
    id: string,
    amount: number,
    type: string,
    description: string,
    date: string,
    email: string,
}

